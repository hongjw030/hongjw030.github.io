import path from 'path';

const DRAWER_WIDTH = 210;
const NAV_HEIGHT = 64;
const MIN_WIDTH = 345;

const THEME_VALUE = 'light';

const POST_DIR = path.join('src/_posts');

export {DRAWER_WIDTH, NAV_HEIGHT, THEME_VALUE, POST_DIR, MIN_WIDTH};